# Used at build time (Expo / web). Must match the API you target.
# Production web (aruru.xyz on Vercel): use the live FastAPI host.
EXPO_PUBLIC_API_URL=https://aruru-backend-production.up.railway.app

# Older Railway hostname (fallback in src/services/api.ts if env unset):
# EXPO_PUBLIC_API_URL=https://aruru-production.up.railway.app
